export const fogOfWarPrompt = `WHAT TO BUILD
Build a portable React + TypeScript client component named FogOfWar. Its optional prop is switches?: { spray?: boolean; remember?: boolean }; a missing value is false. Use it as <FogOfWar switches={{ spray: true, remember: true }} />. It renders a map image that fills its container edge to edge, covered by a field of overlapping dots cleared only by a light that follows the pointer and by the memory of ground already crossed. Spray turns this over: the parchment board stays still and moving the pointer sprays the map onto it in dots that pop in and leave once the pointer is far away. Remember stops crossed ground fogging over and stops sprayed dots leaving. Keep the component, its plain CSS, and its math self-contained. Supply a 1600 × 900 parchment map image with a drawn frame, served at /map-of-middle-earth.jpg. Place the component inside a container with position: relative and a definite height (for example a card at least 360px tall); the map covers that container's padding box.

Reproduce the behavior exactly as specified; do not adjust thresholds, timings, or interaction.

MARKUP AND SEMANTICS
- The root is a div with role="img" and aria-label="Map of Middle-earth". It contains, in order: an SVG, a shade div with aria-hidden="true", and a canvas with aria-hidden="true". There are no controls and no text over the map.
- The SVG has viewBox="100 80 1400 740", preserveAspectRatio="xMidYMid slice", aria-hidden="true", and focusable="false". It holds one image element with href "/map-of-middle-earth.jpg", width 1600, height 900, and preserveAspectRatio="none". The viewBox trims 100 units from the left and right and 80 from the top and bottom, so the image's frame and corner ornaments never show at any container size.

BEHAVIOR
- Constants: DOT_PITCH = 6 CSS pixels, COVER_RADIUS = 4.4 pixels, PIP_RADIUS = 0.9 pixels, PIP_FADE = 0.4, RADIUS_RATIO = 0.3, MIN_RADIUS = 64 pixels, CLEAR_CORE = 0.55, MEMORY_CLARITY = 0.6, NEAR_HALF_LIFE = 6 seconds, FAR_HALF_LIFE = 0.6 seconds, FAR_REACH = 3.5 radii, MEMORY_FLOOR = 0.02, MEMORY_SPREAD = 0.6, PUSH_DISTANCE = 10 pixels, PUSH_WIDTH = 0.5 radii, DOT_STIFFNESS = 170, DOT_DAMPING_RATIO = 0.55 (damping = 2 × 0.55 × √170), POSITION_STIFFNESS = 14 per second, FADE_STIFFNESS = 10 per second, MODE_DURATION = 0.3 seconds, BRUSH_RATIO = 0.7, SPRAY_RATE = 1.2 dots per pixel, CORE_RATIO = 0.4 brush radii, CORE_RATE = 4 dots per pixel, POP_STIFFNESS = 260, POP_DAMPING_RATIO = 0.45 (damping = 2 × 0.45 × √260), FORGET_REACH = 2.5 brush radii, FORGET_SPREAD = 0.4.
- Lattice: measure the root's bounding rectangle (width, height). columns = ceil(width / 6) + 2, rows = ceil(height / 6) + 2. Dot (column, row) has its lattice point at ((column − 0.5) × 6, (row − 0.5) × 6), so one ring of dots lies just outside the edge. The canvas backing store is round(width × devicePixelRatio) by round(height × devicePixelRatio), drawn with a transform scaling by devicePixelRatio. The light radius is max(64, min(width, height) × 0.3). Keep per-dot Float32Arrays: explored, clarity, offset x and y, velocity x and y, spray scale, and spray velocity, all 0; a per-dot Uint8Array spray target, all 0; and a fixed half-life scale. The brush radius is 0.7 × the light radius; the spray carry and the core carry start at 0. Re-measure with a ResizeObserver on the root; each resize recreates the backing store and every array, rebuilds the map layer, steps with dt = 0, and paints.
- Half-life scale: hash(ix, iy) is h = (Math.imul(ix, 374761393) + Math.imul(iy, 668265263)) | 0; h = Math.imul(h ^ (h >>> 13), 1274126177); h ^= h >>> 16; return (h >>> 0) / 4294967295. A dot's scale is 0.7 + 0.6 × hash(column, row).
- Colours: the cover colour is getComputedStyle(root).backgroundColor; the pip colour is getComputedStyle(canvas).color; the shade colour is getComputedStyle(shade).backgroundColor. Re-read all three, rebuild the map layer, and repaint (without stepping) when the document root's data-theme attribute changes (MutationObserver) or when (prefers-color-scheme: dark) changes.
- Map layer: on mount, create an Image, listen for load, then set its src to /map-of-middle-earth.jpg; on load, rebuild the map layer and repaint. Rebuilding does nothing until the image has loaded. It renders into an offscreen canvas of round(width × devicePixelRatio) by round(height × devicePixelRatio) with a transform scaling by devicePixelRatio. Let fit = max(width / 1400, height / 740), x = (width − 1400 × fit) / 2 − 100 × fit, y = (height − 740 × fit) / 2 − 80 × fit. Draw the image at (x, y) with size (1600 × fit, 900 × fit); then fill the whole layer with the shade colour using the multiply composite operation and restore source-over. This matches the SVG and shade exactly. It is never rebuilt per frame.
- Spray mix: a number from 0 (Fog) to 1 (Spray mode), starting at 1 if spray is on at the first render and 0 otherwise, so the component mounts already in its setting. When the spray prop changes, set the mix target to 1 or 0 and wake the loop. Each frame without reduced motion, move the mix toward its target by dt / 0.3, clamped at the target; under reduced motion assign it directly. The drawn blend is the eased mix, m × m × (3 − 2 × m).
- Remember: a flag starting at the remember prop's first-render value. When the prop changes, update the flag and wake the loop. It takes effect at once, with no easing, and does not reset anything: turning it off lets memory decay and sprayed dots leave from where they are.
- Visibility at distance d: 0 if the radius is 0; otherwise t = clamp01((d / radius − 0.55) / 0.45) and visibility = 1 − t × t × (3 − 2 × t). It is 1 across the inner 55% of the radius and 0 at and beyond the radius.
- Memory half-life at distance d from the light: t = clamp01((d / radius − 1) / 2.5), halfLife = 6 + (0.6 − 6) × t × t × (3 − 2 × t). It is 6 seconds out to the radius and 0.6 seconds at 3.5 radii and beyond. Memory decay over dt seconds is 0.5^(dt / (halfLife × the dot's scale)). Under prefers-reduced-motion, or while Remember is on, the decay is always 1.
- Push at distance d: 0 if the radius is 0; otherwise t = (d / radius − 1) / 0.5 and push = 10 × exp(−t²). It peaks at the light's edge.
- Stepping by dt: substeps = ceil(dt / (1/240)), h = dt / substeps. For every dot, let (dx, dy) be its lattice point minus the current light point and d = hypot(dx, dy). lit = visibility(d) × the current light alpha. kept = explored × decay, set to 0 if below 0.02. remembered = max(kept, lit); store it back into explored. clarity = clamp01(max(lit, remembered × 0.6)). The dot's spring target is (dx, dy) / d × push(d) × alpha, or (0, 0) when d is 0. Integrate each axis substeps times with semi-implicit Euler: velocity += (170 × (target − offset) − damping × velocity) × h; offset += velocity × h. If both axes are within 0.05 pixels of the target and both velocities are under 0.5 pixels per second, snap the offset to the target and zero the velocity. Under reduced motion set every offset and velocity to 0 instead. The step reports “active” when any dot has remembered > lit (never under reduced motion or while Remember is on) or any dot did not snap. The light point keeps its last position after the light goes out, so the fog closes toward it.
- Spraying (Spray mode): keep the latest pointer position inside the root, or null. On pointerenter, pointermove, and pointerdown, if a previous position exists and the mix target is 1, add hypot(movement since that position) × 1.2 to the carry; lay floor(carry) dots and keep the fraction as the new carry. To lay a dot, take u = Math.random() and v = Math.random(); its spot is the pointer position plus (brush × u × cos(2πv), brush × u × sin(2πv)), so the spray is densest at the centre; column = round(spot x / 6 + 0.5), row = round(spot y / 6 + 0.5); if both are inside the lattice, set that dot's spray target to 1. On the same event, separately add the same distance × 4 to the core carry, lay floor(core carry) core dots, and keep the fraction. A core dot uses fresh u and v; its spot is the pointer position plus (brush × 0.4 × √u × cos(2πv), brush × 0.4 × √u × sin(2πv)), so it is spread evenly across the core, and it is snapped and laid the same way. Then record the pointer position. On pointerleave and pointercancel, set it to null. A pointer held still lays nothing.
- Forgetting and popping (Spray mode), every frame after stepping: unless Remember is on, for every dot with spray target 1, set its target to 0 if the pointer position is null or the distance from the dot's lattice point to it exceeds brush × 2.5 × (0.8 + 0.4 × hash(column + 911, row + 353)). For every dot whose spray scale or velocity is not already at rest on its target, integrate substeps times with the same h: velocity += (260 × (target − scale) − damping × velocity) × h; scale += velocity × h; then if |target − scale| < 0.005 and |velocity| < 0.05, snap to the target with velocity 0, otherwise report the spray as active. Under reduced motion assign the target directly with velocity 0. The scale overshoots 1 as a dot lands.
- Tracing dots: build one path with a full circle for every dot whose radius is at least 0.05, centred on its lattice point plus its offset (or on the lattice point alone where stated).
- Painting Fog: trace dots of radius (1 − clarity) × 4.4 and fill with the cover colour; then trace dots of radius 0.9 × clamp01((1 − clarity − 0.4) / 0.6) and fill with the pip colour. The SVG map shows through wherever the cover dots leave gaps.
- Painting Spray: fill the whole canvas with the cover colour; trace dots of radius 0.9 on their lattice points alone, without offsets, and fill with the pip colour. Then, if the map layer is built, trace dots of radius max(0, spray scale) × 4.4 on their lattice points alone, clip to that path, draw the map layer over (0, 0, width, height), and restore the clip.
- Painting a frame: clear the canvas. If the blend is below 0.999, paint Fog. If the blend is above 0.001, set globalAlpha to the blend, paint Spray over it, and restore globalAlpha to 1.
- Before the first paint, the canvas has the fog colour as its CSS background so the server render is fully fogged; mark it painted after the first paint and remove that background.
- Light state: current point, target point, alpha, target alpha, all starting at 0. Aiming at a point: if alpha < 0.01, set current to the point immediately; set target to the point, target alpha to 1, and wake the loop.
- Pointer events on the root (pointerenter, pointermove, pointerdown) aim at the pointer's position relative to the root's rectangle. On pointerleave and pointercancel, set target alpha to 0 and wake the loop.
- Animation loop: each frame caps dt at 0.05 seconds. Without reduced motion, move current toward target with blend 1 − exp(−14 × dt), alpha toward target alpha with blend 1 − exp(−10 × dt), and the mix as above. Under reduced motion, assign all three directly. Step with dt, forget and pop with dt, then paint. Request another frame while the light is unsettled (position more than 0.1px from target on either axis, or alpha more than 0.01 from target), while the mix is not at its target, while the step reported active, or while the spray reported active; otherwise let the loop stop. Waking when no frame is pending records performance.now() as the last timestamp and requests a frame. On reduced-motion changes, update the flag and wake the loop.
- On unmount, cancel the pending frame, stop listening for spray changes and for the image's load, disconnect both observers, and remove every listener.

STYLING
- Wherever a colour is written light-dark(a, b), use a in a light scheme and b in a dark one; set color-scheme: light dark on the document root if it is not already set.
- Root: position absolute, inset 0, overflow hidden, border-radius 7px, background light-dark(#ece3cb, #24211b) (the fog colour), touch-action manipulation.
- SVG: position absolute, inset 0, display block, width and height 100%.
- Shade: position absolute, inset 0, background light-dark(transparent, rgb(36 33 27 / 55%)), mix-blend-mode multiply. It darkens the map in a dark scheme and leaves it untouched in a light one.
- Canvas: absolute, inset 0, display block, width and height 100%, color light-dark(#c4b692, #4d473a) (the pip colour), pointer-events none.

DONE WHEN
- The map fills its container with no gap at the edges and none of the image's frame or corner ornaments visible, at any container size.
- At rest the map is covered by solid parchment-coloured fog marked by a regular grid of small pips, in both light and dark schemes, Fog or Spray.
- Fog: moving the pointer clears a light whose radius is 30% of the container's shorter side (at least 64px); the dots at its edge shrink and are shoved outward, and spring back with a visible wobble as the light moves on. Ground left behind keeps smaller dots that grow back unevenly from the outside in, lingering near the light and closing almost at once far from it.
- Spray: moving the pointer sprays speckles of map onto the board around it, sparse on one pass and filling in as the same ground is scrubbed; each speck pops in with a small overshoot. Holding the pointer still lays nothing. Specks spring away once the pointer has moved about two and a half brush radii off, unevenly, and all leave when the pointer leaves the map. The pips beneath never move.
- In Spray, one straight pass fills the ground directly under the pointer almost solid, thinning toward the brush's edge.
- Remember: in Fog, crossed ground stays at its remembered clarity indefinitely; in Spray, sprayed specks stay after the pointer moves away or leaves. Turning it off lets both fade or leave from where they are.
- Toggling spray eases between the two over 0.3 seconds, without resetting the light, memory, or dots.
- The animation loop stops once the light, the memory, the mix, and every dot are at rest.
- In a dark scheme the map is visibly darker than in a light one, in either mode.
- Under reduced motion the dots do not move, the light jumps to its target, toggling spray changes the fog at once, crossed ground stays visible without fading, and sprayed specks appear and disappear without springing.
`;
