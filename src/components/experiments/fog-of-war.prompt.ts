export const fogOfWarPrompt = `WHAT TO BUILD
Build a portable React + TypeScript component named FogOfWar. It takes no props and is used as <FogOfWar />. It renders a pointer-driven visibility field that reveals a contour map through a black fog. Keep the component, its plain CSS, and its math self-contained, with no application-specific imports.

Reproduce the behavior exactly as specified; do not adjust thresholds, timings, or interaction.

MARKUP AND SEMANTICS
- Render a vertical specimen containing one focusable field and one readout paragraph below it.
- The field is a div with role="group", tabIndex="0", aria-label="Fog of war visibility field", and an aria-describedby reference to the field instruction. It is the only interactive element.
- Inside the field, render two canvases in order: a map canvas, then a fog canvas. Both canvases have aria-hidden="true".
- Render an aria-hidden HUD over the canvases. Its top row reads “Visibility field / active” and “Map 04—07”. Its bottom row displays “V(ρ) = max(0, 1−ρ)^0.42 · e^(−0.35ρ²)” and “Dither 4×4”. Render a field instruction reading “Move through the field” and an aria-hidden spotlight ring.
- The readout is a paragraph with aria-live="off" and aria-atomic="true". It contains “Position” with an output that starts as “—”, and “Radius” with an output that reads “188px”.

BEHAVIOR
- Use LIGHT_RADIUS = 188 pixels, FOG_PIXEL_SIZE = 2 pixels, POSITION_STIFFNESS = 13 per second, FADE_STIFFNESS = 18 per second, KEYBOARD_STEP = 24 pixels, and DITHER_SIZE = 4.
- The field's map canvas is drawn at device-pixel-ratio min(window.devicePixelRatio, 2). Set the 2D context transform to that ratio before drawing and restore the identity transform afterward. Fill it with #070907. Draw 1-pixel vertical and horizontal grid lines in rgb(182 198 155 / 8%) starting at 24px and repeating every 48px. Draw seven rotated contour ellipses around (width × 0.5, height × 0.52): rotate the canvas by −0.18 radians, use x radii 50 + ring × 34 and y radii 28 + ring × 20 for ring 1 through 7, and stroke them in rgb(182 198 155 / (11 − ring × 0.9)%). Draw 36 1-pixel map points at ((index × 83) modulo width, (index × 137) modulo height), with rgb(205 224 177 / 28%). Draw “SECTOR 04” at (18, 24), “N  /  07” at (max(18, width − 70), 24), and “37° 46′ N” at (max(18, width − 82), max(30, height − 20)) using 10px ui-monospace, SFMono-Regular, Menlo, monospace in rgb(184 203 158 / 40%).
- The fog canvas is low resolution: its width is ceil(field width × device-pixel-ratio / 2) and its height is ceil(field height × device-pixel-ratio / 2). It is scaled to the field with image-rendering: pixelated. For every fog pixel, sample the centre of that pixel in field CSS pixels and measure d = hypot(sampleX − lightX, sampleY − lightY). Let ρ = max(0, d / 188). If ρ ≥ 1, visibility is 0. Otherwise calculate visibility V(ρ) = clamp01((1 − ρ)^0.42 × exp(−0.35 × ρ²)).
- The continuous fog coverage is 1 − V. To dither only the outer falloff, calculate edgeWeight = smoothstep(0.46, 0.98, ρ) × 0.78. Use the 4×4 Bayer matrix [[0,8,2,10],[12,4,14,6],[3,11,1,9],[15,7,13,5]]. The threshold is (matrix[(pixelY + phase) & 3][(pixelX + phase) & 3] + 0.5) / 16. Calculate quantised = floor(coverage × 8 + threshold) / 8, then alpha = round(clamp01(coverage + (quantised − coverage) × edgeWeight) × 255). The fog pixel is rgb(3 4 3) with that alpha.
- While the field is active, phase is floor(timestamp / 420) modulo 4. Under prefers-reduced-motion, phase is always 0. On every draw, put the image data into the fog canvas, set the spotlight position to the current light point, set its opacity to the current fade alpha, and format the readout position as “(xxx, yyy)” with each coordinate rounded and padded to three digits.
- On pointerenter, pointermove, and pointerdown, convert clientX and clientY to CSS pixels relative to the field's bounding rectangle, clamp them to the field, mark the field active, set the target light to that point, set target alpha to 1, and wake the animation loop. On pointerleave, if the field is not focused, set target alpha to 0 and wake the loop. The current light point and the target light point start at the field centre.
- Each animation frame caps dt at 0.05 seconds. Without reduced motion, move current x and y toward the target with blend = 1 − exp(−13 × dt), and move alpha toward target alpha with blend = 1 − exp(−18 × dt). Stop requesting frames when position is within 0.1px on both axes and alpha is within 0.01 of its target. Under prefers-reduced-motion, assign current = target and alpha = target alpha directly and do not keep a loop alive after a settled draw.
- On focus, keep the light active and, when the pointer is not inside the field, move the target to the field centre. On blur, fade out unless the pointer is still inside. While the field is focused, ArrowLeft and ArrowRight move the target by −24px and +24px on x; ArrowUp and ArrowDown move it by −24px and +24px on y. Clamp the target to the field, prevent the default key action, set target alpha to 1, and wake the loop.
- Re-measure the field with ResizeObserver and window resize. Recreate both canvas backing stores at the new dimensions, redraw the map, clamp current and target to the field, and repaint. Remove every event listener, observer, and pending animation frame when the component unmounts.

STYLING
- Use color-scheme: light dark on the host page if it is not already set. The specimen is a vertical flex column that stretches to its container, has a 12px gap, min-width: 0, and color #dfe7cf.
- The field is position: relative, flex: 1, min-height: 260px, overflow: hidden, border: 1px solid #293426, border-radius: 6px, background: #050605, and box-shadow: inset 0 0 0 1px #111610. It uses cursor: crosshair, isolation: isolate, and touch-action: manipulation. Its focus-visible outline is 2px solid #a9b894 with 4px offset.
- Both canvases are absolute, inset 0, display block, width 100%, height 100%. The map is z-index 0. The fog is z-index 1, pointer-events none, and image-rendering pixelated.
- The HUD is absolute, inset 0, z-index 2, display flex in a column with space-between, padding 15px 16px 14px, pointer-events none, font 9px/1.4 ui-monospace, SFMono-Regular, Menlo, monospace, letter-spacing 0.08em, and uppercase. Its top row is rgb(135 147 122) with strong text rgb(194 206 176) at font-weight 500. Its bottom row is rgb(100 112 93). The formula is rgb(155 168 141), its var and sup are rgb(209 221 189), and it never wraps. At widths up to 600px, HUD padding is 13px, font-size is 8px, and the formula may truncate with ellipsis.
- The instruction is centered absolutely with max-width calc(100% − 48px), uses 10px ui-monospace, letter-spacing 0.13em, uppercase, color rgb(119 132 110), and fades to opacity 0 when the spotlight alpha is greater than 0. Under reduced motion, remove that transition.
- The spotlight is absolute at the current point, 376px square, border 1px solid rgb(211 231 183 / 35%), border-radius 50%, opacity equal to the current fade alpha, and box-shadow 0 0 0 1px rgb(211 231 183 / 8%), 0 0 38px rgb(177 205 139 / 12%), inset 0 0 22px rgb(222 242 193 / 6%). Its centre has a 4px circle in #e3f1ca with a 3px rgb(227 241 202 / 10%) ring and 12px rgb(227 241 202 / 55%) glow. It never receives pointer events.
- The readout is a flex row with space-between, 14px gap, no margin, 10px/1.4 ui-monospace, tabular numerals, uppercase, and color light-dark(#6b6b63, #9c9c91). Each label and value is a flex row with 7px gap; values use light-dark(#282824, #e8e7e0) and never wrap. At widths up to 600px, the field min-height is 245px and the HUD uses the compact values above.

DONE WHEN
- The field is black at rest and shows a centred “Move through the field” instruction. Moving the pointer into it reveals the contour map inside a 188px radius with a soft fade and a visibly ordered dither near the edge.
- The spotlight follows pointer movement with exponential convergence, fades on pointerleave, and shows its current position and fixed radius in the readout.
- Focusing the field reveals the centre light; the four arrow keys move it by 24px steps without scrolling, and the target stays inside the field.
- The map is redrawn on resize, all listeners and animation frames are cleaned up, and reduced motion removes convergence, phase changes, and persistent animation while preserving the same positions and visibility field.
`;
