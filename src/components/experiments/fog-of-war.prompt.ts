export const fogOfWarPrompt = `WHAT TO BUILD
Build a portable React + TypeScript client component named FogOfWar. It takes no props and is used as <FogOfWar />. It renders a survey map that fills its container edge to edge, with six named places that can be selected, all covered by a field of overlapping dots cleared only by a light that follows the pointer and by the memory of ground already crossed. Keep the component, its plain CSS, and its math self-contained. Place it inside a container with position: relative and a definite height (for example a card at least 360px tall); the map covers that container's padding box.

Reproduce the behavior exactly as specified; do not adjust thresholds, timings, or interaction.

MARKUP AND SEMANTICS
- The root is a div with role="group" and aria-label="Map". It contains, in order: an SVG drawing, six place buttons, and a canvas with aria-hidden="true".
- The SVG has viewBox="0 0 480 320", preserveAspectRatio="xMidYMid slice", aria-hidden="true", and focusable="false". Its children, in paint order:
  1. Water: a rect 480 × 320.
  2. Land, three paths: "M130 0C118 34 88 58 94 98C100 136 64 160 70 198C76 238 128 250 152 276C166 292 168 320 168 320L480 320L480 0Z", "M36 250c10-12 34-10 36 3c2 13-18 20-31 15c-8-3-10-10-5-18z", "M52 70c6-8 20-6 21 2c1 8-10 12-18 9c-5-2-6-6-3-11z".
  3. Forest, three paths: "M206 214c18-24 60-22 76-4c16 18 6 48-20 54c-30 6-66-4-64-26c1-10 4-16 8-24z", "M252 30c24-12 58 0 60 22c2 22-24 32-48 28c-22-4-30-36-12-50z", "M404 250c14-10 36-4 38 12c2 14-16 22-30 18c-14-4-20-20-8-30z".
  4. Contours: a group of three ellipses centred at (352, 214) with radii 54 × 32, 36 × 21, and 18 × 10.
  5. A lake, styled as water: an ellipse at (350, 104), radii 44 × 22, transform="rotate(-12 350 104)".
  6. River path: "M308 114C284 138 262 126 238 150S196 166 168 152S120 138 94 132".
  7. Road path: "M152 276C196 258 222 252 254 238S330 196 356 158S418 118 470 50".
  8. Ridge path: "M366 176l9-13l9 13M384 184l8-11l8 11".
  9. Grid path: vertical lines "M{x} 0V320" for x = 40, 80 … 440, then horizontal lines "M0 {y}H480" for y = 40, 80 … 280, joined into one d attribute.
- The places, in order, as (name, map x, map y, label side): “Greyfield” (176, 96, right), “Harrow Ford” (206, 162, right), “The Tarn” (350, 104, left), “Saltmarsh” (122, 226, right), “Low Copse” (244, 236, right), “Old Watch” (352, 214, left).
- Each place is a button type="button" with aria-pressed set to whether it is the selected place. It contains an aria-hidden marker span and a span with the place's name, which is the button's accessible name. At most one place is selected; none is selected initially. Clicking an unselected place selects it; clicking the selected place clears the selection.

BEHAVIOR
- Place positions: the root establishes a size container (container-type: size). Let S = max(100cqw / 480, 100cqh / 320), the scale at which the drawing covers the root. Each place button has inline style left: calc(50% + (x − 240) * S) and top: calc(50% + (y − 160) * S), written out with the numbers substituted, so the marker's centre stays on its map point at any container size while the drawing is cropped.
- Constants: DOT_PITCH = 6 CSS pixels, COVER_RADIUS = 4.4 pixels, PIP_RADIUS = 0.9 pixels, PIP_FADE = 0.4, RADIUS_RATIO = 0.3, MIN_RADIUS = 64 pixels, CLEAR_CORE = 0.55, MEMORY_CLARITY = 0.6, NEAR_HALF_LIFE = 6 seconds, FAR_HALF_LIFE = 0.6 seconds, FAR_REACH = 3.5 radii, MEMORY_FLOOR = 0.02, MEMORY_SPREAD = 0.6, PUSH_DISTANCE = 10 pixels, PUSH_WIDTH = 0.5 radii, DOT_STIFFNESS = 170, DOT_DAMPING_RATIO = 0.55 (damping = 2 × 0.55 × √170), POSITION_STIFFNESS = 14 per second, FADE_STIFFNESS = 10 per second.
- Lattice: measure the root's bounding rectangle (width, height). columns = ceil(width / 6) + 2, rows = ceil(height / 6) + 2. Dot (column, row) has its lattice point at ((column − 0.5) × 6, (row − 0.5) × 6) in root coordinates, so one ring of dots lies just outside the edge. The canvas backing store is round(width × devicePixelRatio) by round(height × devicePixelRatio), drawn with a transform scaling by devicePixelRatio. The light radius is max(64, min(width, height) × 0.3). Keep per-dot Float32Arrays: explored, clarity, offset x and y, and velocity x and y, all 0, plus a fixed half-life scale. Re-measure with a ResizeObserver on the root; each resize recreates the backing store and every array, steps with dt = 0, and paints.
- Half-life scale: hash(ix, iy) is h = (Math.imul(ix, 374761393) + Math.imul(iy, 668265263)) | 0; h = Math.imul(h ^ (h >>> 13), 1274126177); h ^= h >>> 16; return (h >>> 0) / 4294967295. A dot's scale is 0.7 + 0.6 × hash(column, row).
- Colours: the cover colour is getComputedStyle(root).backgroundColor; the pip colour is getComputedStyle(canvas).color. Re-read both and repaint (without stepping) when the root element of the document's data-theme attribute changes (MutationObserver) or when (prefers-color-scheme: dark) changes.
- Visibility at distance d: 0 if the radius is 0; otherwise t = clamp01((d / radius − 0.55) / 0.45) and visibility = 1 − t × t × (3 − 2 × t). It is 1 across the inner 55% of the radius and 0 at and beyond the radius.
- Memory half-life at distance d from the light: t = clamp01((d / radius − 1) / 2.5), halfLife = 6 + (0.6 − 6) × t × t × (3 − 2 × t). It is 6 seconds out to the radius and 0.6 seconds at 3.5 radii and beyond. Memory decay over dt seconds is 0.5^(dt / (halfLife × the dot's scale)). Under prefers-reduced-motion the decay is always 1.
- Push at distance d: 0 if the radius is 0; otherwise t = (d / radius − 1) / 0.5 and push = 10 × exp(−t²). It peaks at the light's edge.
- Stepping by dt: substeps = ceil(dt / (1/240)), h = dt / substeps. For every dot, let (dx, dy) be its lattice point minus the current light point and d = hypot(dx, dy). lit = visibility(d) × the current light alpha. kept = explored × decay, set to 0 if below 0.02. remembered = max(kept, lit); store it back into explored. clarity = clamp01(max(lit, remembered × 0.6)). The dot's spring target is (dx, dy) / d × push(d) × alpha, or (0, 0) when d is 0. Integrate each axis substeps times with semi-implicit Euler: velocity += (170 × (target − offset) − damping × velocity) × h; offset += velocity × h. If both axes are within 0.05 pixels of the target and both velocities are under 0.5 pixels per second, snap the offset to the target and zero the velocity. Under reduced motion set every offset and velocity to 0 instead. The step reports “active” when any dot has remembered > lit (never under reduced motion) or any dot did not snap. The light point keeps its last position after the light goes out, so the fog closes toward it.
- Painting: clear the canvas. Build one path of full circles at each dot's lattice point plus its offset with radius (1 − clarity) × 4.4, skipping radii under 0.05, and fill it with the cover colour. Then build a second path of circles at the same centres with radius 0.9 × clamp01((1 − clarity − 0.4) / 0.6), with the same skip, and fill it with the pip colour.
- Before the first paint, the canvas has the fog colour as its CSS background so the server render is fully fogged; mark it painted after the first paint and remove that background.
- Light state: current point, target point, alpha, target alpha, all starting at 0. Aiming at a point: if alpha < 0.01, set current to the point immediately; set target to the point, target alpha to 1, and wake the loop.
- Pointer events on the root (pointerenter, pointermove, pointerdown) mark the pointer as inside and aim at the pointer's position relative to the root's rectangle. On pointerleave and pointercancel, mark the pointer outside; then, if the focused element is inside the root and matches :focus-visible, aim at that element's centre; otherwise set target alpha to 0 and wake the loop.
- On focusin, if the pointer is not inside, aim at the focused element's centre when it is inside the root and matches :focus-visible. On focusout, if the pointer is not inside and focus is moving outside the root, set target alpha to 0 and wake the loop.
- Animation loop: each frame caps dt at 0.05 seconds. Without reduced motion, move current toward target with blend 1 − exp(−14 × dt) and alpha toward target alpha with blend 1 − exp(−10 × dt). Under reduced motion, assign both directly. Step with dt, then paint. Request another frame while the light is unsettled (position more than 0.1px from target on either axis, or alpha more than 0.01 from target) or while the step reported active; otherwise let the loop stop. Waking when no frame is pending records performance.now() as the last timestamp and requests a frame. On reduced-motion changes, update the flag and wake the loop.
- On unmount, cancel the pending frame, disconnect both observers, and remove every listener.

STYLING
- Inherit the page's sans-serif font. Wherever a colour is written light-dark(a, b), use a in a light scheme and b in a dark one; set color-scheme: light dark on the document root if it is not already set.
- Root: position absolute, inset 0, overflow hidden, border-radius 7px, background light-dark(#f4f4ef, #1d1d1a) (the fog colour), color light-dark(#282824, #e8e7e0), container-type size, touch-action manipulation.
- SVG: position absolute, inset 0, display block, width and height 100%.
- Water (rect and lake): fill light-dark(#d5dfdf, #1a2326). Land: fill light-dark(#ebe7d9, #262520), stroke light-dark(#b7c6c6, #2e3b3e), stroke-width 1.5. Forest: fill light-dark(#d3dbc2, #2c3326). Contours: no fill, stroke light-dark(#d2cbb5, #3a382f), width 1. River: no fill, stroke light-dark(#b7c6c6, #34454a), width 3, round caps. Road: no fill, stroke light-dark(#a39a82, #6b6656), width 1.5, dasharray 5 4. Ridge: no fill, stroke light-dark(#8f8a78, #7a7566), width 1.5, round joins. Grid: no fill, stroke light-dark(rgb(40 40 36 / 6%), rgb(232 231 224 / 5%)), width 1.
- Place button: position absolute, display flex, items centred, gap 6px, min-height 24px, padding 4px 8px 4px 4px, no border, border-radius 4px, transparent background, inherited colour and font, 11.5px, weight 500, line-height 1.2, nowrap, transform translate(−9px, −50%). A place whose label side is left uses flex-direction row-reverse, padding 4px 4px 4px 8px, and transform translate(calc(−100% + 9px), −50%). Either way the marker's centre sits on the place's point.
- Marker: flex none, 10px circle, background light-dark(#30312b, #e8e7e0), box-shadow 0 0 0 2px light-dark(#ebe7d9, #262520), transitions on background and box-shadow of 140ms ease. Name: text-shadow 0 0 2px and 0 0 4px, both light-dark(#ebe7d9, #262520). Hovering a place underlines its name with a 2px offset.
- Selected place (aria-pressed true): colour light-dark(#54614a, #b6c2a2); its marker background is the same colour, with box-shadow 0 0 0 2px light-dark(#ebe7d9, #262520), 0 0 0 4px light-dark(#667251, #8e9c78).
- Place focus-visible: outline 2px solid light-dark(#667251, #8e9c78), offset 2px.
- Canvas: absolute, inset 0, display block, width and height 100%, color light-dark(#a9aa9f, #62635a) (the pip colour), pointer-events none.
- Under prefers-reduced-motion, remove the marker transitions.

DONE WHEN
- The map fills its container with no gap at the edges, and at rest it is covered by solid fog marked by a regular grid of small pips, in both light and dark schemes.
- Resizing the container crops the drawing around its centre and every place's marker stays on its landmark.
- Moving the pointer over the map clears a light whose radius is 30% of the container's shorter side (at least 64px); the dots at its edge shrink and are shoved outward, and spring back with a visible wobble as the light moves on.
- Ground left behind keeps smaller dots that grow back unevenly from the outside in, lingering near the light and closing almost at once far from it.
- The animation loop stops once the light, the memory, and every dot are at rest.
- Every place can be selected through the fog, at most one at a time, and tabbing to one from the keyboard lights it.
- Under reduced motion the dots do not move, the light jumps to its target, and crossed ground stays visible without fading.
`;
